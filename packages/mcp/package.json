{
  "name": "@dagshub-ai-dev-plaform-support/mcp",
  "private": true,
  "version": "0.0.0",
  "description": "Model Context Protocol server with FastMCP",
  "scripts": {
    "dev": "uv run uvicorn src.api:app --host 0.0.0.0 --port 3001 --reload",
    "start": "uv run uvicorn src.api:app --host 0.0.0.0 --port 3001",
    "test": "uv run pytest",
    "lint": "uv run ruff check . && uv run ruff format --check .",
    "lint:fix": "uv run ruff check --fix . && uv run ruff format .",
    "install:deps": "uv sync --all-extras"
  }
}