{{- if .Values.api.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.api.name }}
  labels:
    {{- include "dagshub-ai-dev-plaform-support.api.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.api.replicas }}
  selector:
    matchLabels:
      {{- include "dagshub-ai-dev-plaform-support.api.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "dagshub-ai-dev-plaform-support.api.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "dagshub-ai-dev-plaform-support.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: api
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ include "dagshub-ai-dev-plaform-support.image" (dict "name" .Values.api.image.repository "tag" .Values.api.image.tag "Values" .Values) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            {{- if .Values.pgvector.enabled }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "dagshub-ai-dev-plaform-support.fullname" . }}-secret
                  key: DATABASE_URL
            {{- end }}
            - name: DEBUG
              valueFrom:
                secretKeyRef:
                  name: {{ include "dagshub-ai-dev-plaform-support.fullname" . }}-secret
                  key: DEBUG
            - name: ALLOWED_HOSTS
              valueFrom:
                secretKeyRef:
                  name: {{ include "dagshub-ai-dev-plaform-support.fullname" . }}-secret
                  key: ALLOWED_HOSTS
            - name: LLM_PROVIDER
              valueFrom:
                secretKeyRef:
                  name: {{ include "dagshub-ai-dev-plaform-support.fullname" . }}-secret
                  key: LLM_PROVIDER
            - name: LLM_MODEL
              valueFrom:
                secretKeyRef:
                  name: {{ include "dagshub-ai-dev-plaform-support.fullname" . }}-secret
                  key: LLM_MODEL
            - name: LLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "dagshub-ai-dev-plaform-support.fullname" . }}-secret
                  key: LLM_API_KEY
            - name: MCP_ENABLED
              valueFrom:
                secretKeyRef:
                  name: {{ include "dagshub-ai-dev-plaform-support.fullname" . }}-secret
                  key: MCP_ENABLED
            - name: MCP_SSE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "dagshub-ai-dev-plaform-support.fullname" . }}-secret
                  key: MCP_SSE_URL
            - name: LLAMASTACK_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "dagshub-ai-dev-plaform-support.fullname" . }}-secret
                  key: LLAMASTACK_URL
          {{- if .Values.api.healthCheck.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.api.healthCheck.path }}
              port: http
            initialDelaySeconds: {{ .Values.api.healthCheck.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.healthCheck.periodSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.api.healthCheck.path }}
              port: http
            initialDelaySeconds: {{ .Values.api.healthCheck.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.healthCheck.periodSeconds }}
          {{- end }}
          resources:
            {{- toYaml .Values.api.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
